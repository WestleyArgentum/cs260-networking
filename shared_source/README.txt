There is a problem at the moment getting the server to link into the shared file folder (if it
has not already been built).

For now the shared files are in the server and the client (who has no such problem for reasons
unbenounced to me) links into them.